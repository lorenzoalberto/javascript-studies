events {}

http {
    # TODO: crie o upstream api_backend com as três instâncias.

    server {
        listen 8080;
        server_name localhost;

        # TODO: encaminhe /api/ para o upstream.
        # TODO: configure os headers do proxy.
    }
}

