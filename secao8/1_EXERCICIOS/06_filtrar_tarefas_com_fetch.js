/*
EXERCÍCIO: Buscar e filtrar tarefas com Fetch

Crie a função async `buscarTarefasConcluidas(usuarioId)`. Consulte:
https://jsonplaceholder.typicode.com/todos?userId=ID

Depois de validar a resposta HTTP e converter o JSON:
- mantenha somente tarefas com `completed` igual a true;
- transforme cada tarefa em `{ id, titulo }`;
- retorne o novo array.

Entrada: buscarTarefasConcluidas(1)

Saída esperada:
Um array com 11 tarefas concluídas do usuário 1. Cada objeto deve possuir apenas
as propriedades `id` e `titulo`.

Ao final, exiba as tarefas e a mensagem:
Total concluídas: 11
*/

async function buscarTarefasConcluidas(usuarioId) {
    // TODO: busque, valide, filtre, mapeie e retorne as tarefas.
}

// TODO: execute a busca dentro de try/catch e exiba o resultado.

