/*
EXERCÍCIO: Login com async/await

Complete `autenticar(email, senha)`, que retorna uma Promise:

- resolva com `{ id: 1, nome: "Ana" }` quando o email for "ana@email.com" e a
  senha for "1234";
- rejeite com um Error de mensagem "Credenciais inválidas." nos demais casos.

Depois, implemente a função async `executarLogin`. Use `await` dentro de
`try/catch` e exiba uma das mensagens:

Login realizado: Ana
Erro no login: Credenciais inválidas.

Teste uma combinação válida e outra inválida.
*/

function autenticar(email, senha) {
    // TODO: retorne a Promise de autenticação.
}

async function executarLogin(email, senha) {
    // TODO: aguarde o resultado e trate a rejeição.
}

// TODO: execute os dois casos de teste.

