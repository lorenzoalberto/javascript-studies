/*
EXERCÍCIO: Promessa de entrega

Crie a função `verificarEntrega(status)`, que retorna uma Promise. Após 500 ms:

- se `status` for "entregue", resolva com "Pedido entregue com sucesso!";
- caso contrário, rejeite com "O pedido ainda não foi entregue.".

Consuma a Promise usando `.then()` e `.catch()`.

Entradas e saídas esperadas:
verificarEntrega("entregue") -> Pedido entregue com sucesso!
verificarEntrega("em transporte") -> O pedido ainda não foi entregue.

Teste um caso de cada vez para observar os dois caminhos.
*/

function verificarEntrega(status) {
    // TODO: crie e retorne a Promise usando setTimeout.
}

// TODO: consuma um caso com .then() e .catch().

