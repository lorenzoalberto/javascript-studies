/*
EXERCÍCIO: Maior e menor valor

Use um laço `for` para encontrar o maior e o menor número do array. Não use
`Math.max()`, `Math.min()` ou `sort()`.

Saída esperada:
Menor: -4
Maior: 21

Desafio extra: exiba também os índices em que esses valores aparecem.
*/

const numeros = [12, 5, -4, 21, 8, 3];
let menor = numeros[0];
let maior = numeros[0];

// TODO: compare cada número com os valores armazenados em `menor` e `maior`.

console.log(`Menor: ${menor}`);
console.log(`Maior: ${maior}`);
