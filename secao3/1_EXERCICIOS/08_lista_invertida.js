/*
EXERCÍCIO: Lista invertida

Use um laço `for` decrescente para percorrer o array do último elemento até o
primeiro. Adicione os elementos em `ordemInvertida` sem usar `reverse()`.

Saída esperada:
["roxo", "verde", "azul", "vermelho"]

Desafio extra: faça o mesmo com uma string e monte uma palavra invertida.
*/

const cores = ["vermelho", "azul", "verde", "roxo"];
const ordemInvertida = [];

// TODO: percorra `cores` de trás para frente e preencha `ordemInvertida`.

console.log(ordemInvertida);
